{
  "name": "wakra-project",
  "description": "Project Management System",
  "private": true,
  "scripts": {
    "kill-ports": "kill-port --port 3030 && kill-port --port 6060",
    "bootstrap": "lerna bootstrap",
    "start": "yarn kill-port && lerna run start --parallel --scope *",
    "eslint": "lerna eslint"
  },
  "devDependencies": {
    "kill-port": "^1.6.1",
    "lerna": "^4.0.0"
  },
  "changelog": {
    "labels": {
      "feat": "New Features",
      "fix": "Bug Fix",
      "tech": "Tech"
    }
  }
}
